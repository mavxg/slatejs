//Table commands.

function commands(editor) {
	return {
		//
	};
}

var keymap = [];

var context_menu = [
	//insert table
	//insert col
	//insert row
	//etc.
];

module.exports = {
	commands: commands,
	keymap: keymap,
	menu: [],
	context_menu: context_menu,
};